import { CardinalToOrdinalPipe } from './cardinal-to-ordinal.pipe';

describe('CardinalToOrdinalPipe', () => {
  it('create an instance', () => {
    const pipe = new CardinalToOrdinalPipe();
    expect(pipe).toBeTruthy();
  });
});
